{
  "name": "quick-draw-samurai-playable",
  "private": true,
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "typecheck": "tsc --noEmit",
    "build": "npm run clean && tsc"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
